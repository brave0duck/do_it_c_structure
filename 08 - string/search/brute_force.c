// 문자열내 패턴검색
// 1. brute force (단순무식방법)
// 배열처음부터 마지막까지 패턴문자열과 일치하는지 순서대로 비교
#include <stdio.h>
#include <string.h>
// src = "string"
// find = "in"
int brute_force(const char* src, const char * find){
    int src_size = strlen(src);
    int fnd_size = strlen(find);
    
    for(int i=0;)
}
int main(void){

    return 0;
}