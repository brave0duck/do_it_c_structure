#include <stdio.h>

// check member
int IsMember(BitSet s, int n);
// add member
void Add(BitSet *s,int n);
// remove member
void Remove(BitSet *s, int n);
// size
int Size(BitSet s);
//print
void Print(BitSet s);
// print line
void Print(BitSet s);

int main(void){

    return 0;
}